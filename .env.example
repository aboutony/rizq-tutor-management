
# PostgreSQL connection string
# Example: postgresql://user:password@host:port/database
POSTGRES_URL=""

# Secret for signing JWT session tokens. Generate a strong random string.
# A quick way to generate one is: openssl rand -base64 32
JWT_SECRET=""

# The public base URL of the application, used for generating links in dev logs.
# Example: http://localhost:3000
NEXT_PUBLIC_BASE_URL="http://localhost:3000"
